node_modules
dist
dist-ssr
*.local
.env
.DS_Store
*.log

# Build artifacts
tsconfig.tsbuildinfo

# Local-only media (personal photos and unused audio samples)
Input/
resources/

# Downloaded at build time — bundled inside the installer (do not commit)
src-tauri/resources/exiftool/

# Accidental ExifTool output in src-tauri root
src-tauri/*.jpg
